# ChatServer configuration
# Priority: defaults -> this file -> command line flags
# API keys are read from environment variables only.

port=8080
log_level=INFO
listen_addr=0.0.0.0
temperature=0.7
max_tokens=2048
ollama_model_name=deepseek-r1:1.5b
ollama_model_desc=Deepseek系列中最小巧的推理模型，尽管规模小，但在数学和逻辑等任务上显著优于同尺寸的传统模型。
ollama_endpoint=http://192.168.58.1:11434
